1. in the uploadiMAGE
WE CAN SIMPLY USE UPLOADER FUNCTION TEN WHY WE NEED A PROMISE?
upload()
 └── callback()
       └── return result
       function s lik this when u will return result it will return the callbak and not the outer function

upload(buffer)
│
├─ Create Promise
│
├─ Create Cloudinary upload stream
│
├─ Send buffer
│     stream.end(buffer)
│
├─ Upload happening...
│
├─ Cloudinary finishes
│
├─ Callback runs
│
├─ Success?
│      └─ resolve(result)
│
└─ Failure?
       └─ reject(error)
       But await only works with Promises, not callbacks.
       CALL ME LATER  (callback)
        ↓
PROMISE
        ↓
I'LL WAIT (await)

What does upload.single("image") do?


It reads the incoming file and adds it to the request object:
req.file = {
  originalname: "cat.jpg",
  buffer: ...,
  mimetype: "image/jpeg"
}Request with image
        ↓
Controller
        ↓
req.file = undefined ❌
Request with image
        ↓
upload.single("image")
        ↓
req.file created
        ↓
Controller
        ↓
req.file available ✅

sharp is an npm package used to wrap high performance livips imag library and desines to be fast and memory efficient.
Sharp works with buffer
let transformed = sharp(buffer);

Now Sharp loads the image into memory.
let outputBuffer: Buffer;

We'll store the transformed image here.
case "png":
  outputBuffer =
    await transformed
      .png()
      .toBuffer();image to png convert

